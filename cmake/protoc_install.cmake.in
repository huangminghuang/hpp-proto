if(NOT EXISTS "@HPP_PROTO_PROTOC_ZIP_PATH@")
  message(FATAL_ERROR "protoc zip not found at @HPP_PROTO_PROTOC_ZIP_PATH@")
endif()
file(ARCHIVE_EXTRACT INPUT "@HPP_PROTO_PROTOC_ZIP_PATH@" DESTINATION "${CMAKE_INSTALL_PREFIX}")
