---
name: Conan

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - develop
      - dev/**
    paths-ignore:
      - '**.md'
  pull_request:
    branches:
      - main
      - develop
    paths-ignore:
      - '**.md'

env:
  CTEST_OUTPUT_ON_FAILURE: 1
  PROTOC_VERSION: "35.0"
  PROTOC_ZIP: protoc-35.0-linux-x86_64.zip
  PROTOC_SHA256: a45cda0989c17dd950db55f6fbe1e5814c50fda08e87aa422980ac1f89dddbbc
  CCACHE_DIR: ${{ github.workspace }}/.ccache

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: conan-protobuf-binary-shared
            build_cppstd: gnu17
            use_system_protobuf: false
            with_protobuf: "True"
            build_shared_libs: "ON"
          - name: system-protobuf-cpp23-static
            build_cppstd: 23
            use_system_protobuf: true
            with_protobuf: "False"
            build_shared_libs: "OFF"
    name: ${{ matrix.name }}
    env:
      CC: gcc-13
      CXX: g++-13

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: setup cpp tools
        uses: aminya/setup-cpp@8170d66c458f4a045220b7b0966c10940bb2a15d # v1.8.1
        with:
          compiler: gcc-13
          cmake: "4.4.0"
          ninja: true
          ccache: true
          conan: "2.30.0"

      - name: Cache ccache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ github.workflow }}-${{ runner.os }}-${{ matrix.name }}-ccache-${{ github.sha }}
          restore-keys: |
            ${{ github.workflow }}-${{ runner.os }}-${{ matrix.name }}-ccache-

      - name: install system protobuf
        if: matrix.use_system_protobuf
        run: |
          sudo apt-get install -y protobuf-compiler libprotobuf-dev
          echo "PROTOC_BIN=/usr/bin" >> "$GITHUB_ENV"

      - name: conan profile
        run: conan profile detect --force

      - name: create package
        run: |
          conan create . \
            -s:h compiler.cppstd=23 \
            -s:b arch=x86_64 -s:b compiler=gcc -s:b compiler.version=13 -s:b compiler.libcxx=libstdc++11 \
            -s:b compiler.cppstd=${{ matrix.build_cppstd }} \
            -o '&:with_protobuf=${{ matrix.with_protobuf }}' \
            --build=missing

      - name: install dependencies
        run: |
          conan install tutorial \
            -s:h build_type=Release \
            -s:b build_type=Release \
            -s:h compiler.cppstd=23 \
            -s:h protobuf/*:compiler.cppstd=gnu17 \
            -s:b arch=x86_64 -s:b compiler=gcc -s:b compiler.version=13 -s:b compiler.libcxx=libstdc++11 \
            -s:b compiler.cppstd=${{ matrix.build_cppstd }} \
            -o 'hpp_proto/*:with_protobuf=${{ matrix.with_protobuf }}' \
            --build=missing \
            -of tutorial-build

      - name: configure
        if: matrix.use_system_protobuf
        run: |
          cmake -S tutorial --preset conan-release -DCMAKE_PROGRAM_PATH="$PROTOC_BIN" -DBUILD_SHARED_LIBS=${{ matrix.build_shared_libs }}

      - name: configure
        if: ${{ !matrix.use_system_protobuf }}
        run: |
          cmake -S tutorial --preset conan-release -DBUILD_SHARED_LIBS=${{ matrix.build_shared_libs }}

      - name: build
        run: |
          cd tutorial
          cmake --build --preset conan-release -j2

      - name: test
        run: |
          cd tutorial
          ctest --preset conan-release --output-on-failure
